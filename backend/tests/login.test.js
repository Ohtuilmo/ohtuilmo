const { describe, test } =  require('jest')

describe('Login', () => {
  test.todo('is successful with existing student number')
  test.todo('is successful with new student number')
  test.todo('fails with missing student number')
})
