const { describe, test } =  require('@jest/globals')

describe('Groups', () => {
  test.todo('should be gotten with credentials')
  test.todo('query should fails with invalid credentials')
  test.todo('shouldn\'t be created with invalid credentials')
  test.todo('shouldn\'t be created with incorrect data')
  test.todo('should be created with correct data and credentials')
})
